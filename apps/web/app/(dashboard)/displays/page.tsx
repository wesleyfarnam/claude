import { ComingSoon } from "@/components/ui/ComingSoon";

export default function DisplaysPage() {
  return (
    <ComingSoon
      eyebrow="Displays"
      title="Compose the screen, zone by zone"
      milestone="M4"
      body="Drag-and-drop canvas using @dnd-kit. Percentage-based zones. Drop in media, weather, headlines, a clock. The same JSON renders pixel-perfect on every stick."
    />
  );
}
