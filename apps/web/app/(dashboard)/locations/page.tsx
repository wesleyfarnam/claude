import { ComingSoon } from "@/components/ui/ComingSoon";

export default function LocationsPage() {
  return (
    <ComingSoon
      eyebrow="Locations"
      title="Your retail footprint"
      milestone="M1"
      body="Create locations, assign managers, and group devices by store. CRUD page coming next, backed by `locations` + `location_managers`."
    />
  );
}
