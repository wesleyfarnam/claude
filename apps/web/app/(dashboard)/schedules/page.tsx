import { ComingSoon } from "@/components/ui/ComingSoon";

export default function SchedulesPage() {
  return (
    <ComingSoon
      eyebrow="Schedules"
      title="Right content, right time"
      milestone="M5"
      body="Day-parting, date ranges, per-location timezones, priority overrides. The server computes the currently-playing playlist for each device on demand."
    />
  );
}
